name: Hello World
on:
  push:
    branches:
      - main
jobs:
  first-hello-wld-job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: print sms
        user: echo "hello-world"

