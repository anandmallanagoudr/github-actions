name: Hello World
on:
   push:
     branch:
      - main
jobs:
  First-Hallow-World-Job:
    runs-on: ubuntu:latest
    steps:
      - name: checkout code
        user: action/checkout@v3
      - name: Print message
        run: echo "hello-world"
